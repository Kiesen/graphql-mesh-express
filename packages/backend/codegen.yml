overwrite: true
schema: 'http://localhost:4848/graphql'
documents: null
generates:
  src/mesh/types/generated.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
    config:
      allowParentTypeOverride: true
    hooks:
      afterOneFileWrite:
        - prettier --write
