# This mutation is supposed to fail because there is no
# corresponding `logDB` mutation submitted alongside the `updateTodoContent`
mutation MutationTryInvalid {
  live_updateTodoContent(content: "New content") {
    id
    content
  }
}
