# This mutation is supposed to fail because there is no
# corresponding `logDB` mutation submitted alongside the `updateManagerFirstName`
mutation MutationTryInvalid {
  updateManagerFirstName(firstName: "newFirstName") {
    firstName
    lastName
  }
}
