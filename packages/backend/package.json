{
  "name": "backend",
  "version": "0.1.0",
  "private": false,
  "license": "MIT",
  "scripts": {
    "start": "nodemon",
    "build": "rimraf ./build && tsc",
    "lint": "eslint --ext .ts --ext .tsx --ext .js --max-warnings=0 src"
  },
  "devDependencies": {
    "nodemon": "^2.0.7",
    "rimraf": "^3.0.2",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.2"
  },
  "dependencies": {
    "@graphql-mesh/config": "^0.14.1",
    "@graphql-mesh/merger-stitching": "^0.7.34",
    "@graphql-mesh/mysql": "^0.7.1",
    "express": "^4.17.1",
    "express-graphql": "^0.12.0",
    "mysql": "^2.18.1"
  }
}
