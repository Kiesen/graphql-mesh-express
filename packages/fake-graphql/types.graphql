type Manager {
  id: ID @fake(type: uuid)
  firstName: String! @fake(type: firstName)
  lastName: String! @fake(type: lastName)
  phoneNumber: String! @fake(type: phoneNumber)
}

type Query {
  manager: Manager!
}

type Mutation {
  updateManagerFirstName(firstName: String!): Manager
}
